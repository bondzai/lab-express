section1: heroku
    git-sync
        git add .
        git commit -m "something changed"
        git push heroku master
        
    setting
        config variables
            port = process.env.port || 4000
    
    process.env

section2: API docs request & response
    swagger-ui-express
    YAML is a little bit cleaner than json
        npm i yamljs
        issue : nodemon can not monitoring yaml file => {
            solve : add nodemon.json
        }
    Authentication token for swagger docs
    Handling many variables type in swagger
        String get request 
        Object get request
        Array get request

    info : app.use(express.json()) = bodypraser middle ware embedded in express 

    learn : query request
    learn : upload image
    learn : request headers token

    request
        input from url, body, query & images

    response
        string ,json, status code

    issue : 
        file got big -> multi dev environment
        nothing is permanent
        inject database
        hide secrets

section3:
    hiding the secrets
        dotenv
            require('dotenv').config()
            require('dotenv').config({ path: 'custom/.env'})
    database
        ORM: Object Relation Mapper
        ODM: Object Data Mapper
        mongoose
        theory : food -> process -> cool -> digest -> goes
        good data have to processing first before go in to database
        structure === schema
        Focus on medeling rather than syntax
        Modeling design
        
        ***express can not handle json file directly, it's must use middleware
        learn : how to encryp the password #262A2C

        learn : middleware (to protect the route)
            Next() keyword

        learn : token
            token = header + payload + signature
        
        learn : login flow

        cookie  - http only cookies for backend
                - nomal cookies for everyone
                - embedded token in cookies

        learn : write the custom middleware